<UserControl x:Class="MCPhoto.App.Views.DoneView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="720" d:DesignWidth="1280">
    <Grid Background="{StaticResource Brush.Bg}">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="감사합니다!" Style="{StaticResource Text.Display}" FontSize="48"
                       HorizontalAlignment="Center" />
            <TextBlock Text="즐거운 추억 되세요" Style="{StaticResource Text.H2}"
                       Foreground="{StaticResource Brush.Text.Tertiary}"
                       HorizontalAlignment="Center" Margin="0,12,0,40" />
            <Button Content="처음으로" Command="{Binding GoHomeCommand}"
                    Style="{StaticResource Button.Primary}"
                    Padding="48,0" HorizontalAlignment="Center" />
        </StackPanel>
    </Grid>
</UserControl>
