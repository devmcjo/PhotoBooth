<UserControl x:Class="MCPhoto.App.Views.AccountView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="900" d:DesignWidth="1280">
    <Grid Background="{StaticResource Brush.Bg}">
        <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="40,88,40,40">
            <StackPanel MaxWidth="560" HorizontalAlignment="Center">

                <TextBlock Text="{Binding Title}" Style="{StaticResource ScreenTitle}" />

                <!-- ══════════ ① 내 계정 정보 (읽기 전용) it15 §6.3 ══════════ -->
                <Border Style="{StaticResource Card}" Margin="0,0,0,16"
                        Visibility="{Binding IsAccount, Converter={StaticResource BoolToVis}}">
                    <StackPanel>
                        <TextBlock Text="내 계정 정보" Style="{StaticResource Text.H2}" Margin="0,0,0,16" />
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" MinWidth="120" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Grid.Column="0" Text="아이디" Style="{StaticResource Text.Label}" Margin="0,0,16,8" />
                            <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding AccountId}" Style="{StaticResource Text.Body}" Margin="0,0,0,8" />

                            <TextBlock Grid.Row="1" Grid.Column="0" Text="이메일" Style="{StaticResource Text.Label}" Margin="0,0,16,8" />
                            <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding AccountEmail}" Style="{StaticResource Text.Body}"
                                       TextWrapping="Wrap" Margin="0,0,0,8" />

                            <TextBlock Grid.Row="2" Grid.Column="0" Text="로그인 방식" Style="{StaticResource Text.Label}" Margin="0,0,16,8" />
                            <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding AuthMethodLabel}" Style="{StaticResource Text.Body}" Margin="0,0,0,8" />

                            <TextBlock Grid.Row="3" Grid.Column="0" Text="역할" Style="{StaticResource Text.Label}" Margin="0,0,16,8" />
                            <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding RoleLabel}" Style="{StaticResource Text.Body}" Margin="0,0,0,8" />

                            <TextBlock Grid.Row="4" Grid.Column="0" Text="가입일" Style="{StaticResource Text.Label}" Margin="0,0,16,0" />
                            <TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding CreatedAtText}" Style="{StaticResource Text.Body}" />
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- ══════════ ② PIN 변경 (계정 관리 모드) it15 §6.3 ══════════ -->
                <Border Style="{StaticResource Card}" Margin="0,0,0,16"
                        Visibility="{Binding IsAccount, Converter={StaticResource BoolToVis}}">
                    <StackPanel>
                        <TextBlock Text="PIN 변경" Style="{StaticResource Text.H2}" Margin="0,0,0,8" />
                        <TextBlock Text="설정·계정 관리 진입 시 사용하는 4자리 숫자 PIN입니다."
                                   Style="{StaticResource Text.Caption}" Foreground="{StaticResource Brush.Text.Muted}"
                                   TextWrapping="Wrap" Margin="0,0,0,16" />

                        <!-- 현재 PIN(이미 설정된 경우에만 노출). HasPin=false(최초 설정)면 숨김. -->
                        <StackPanel Visibility="{Binding HasPin, Converter={StaticResource BoolToVis}}">
                            <TextBlock Text="현재 PIN" Style="{StaticResource Text.Label}" Margin="0,0,0,4" />
                            <PasswordBox x:Name="CurrentPinBox" MaxLength="4" Margin="0,0,0,12"
                                         PasswordChanged="OnCurrentPinChanged" PreviewTextInput="OnPinPreviewTextInput" />
                        </StackPanel>

                        <TextBlock Text="새 PIN (4자리 숫자)" Style="{StaticResource Text.Label}" Margin="0,0,0,4" />
                        <PasswordBox x:Name="NewPinBox" MaxLength="4" Margin="0,0,0,12"
                                     PasswordChanged="OnNewPinChanged" PreviewTextInput="OnPinPreviewTextInput" />
                        <TextBlock Text="새 PIN 확인" Style="{StaticResource Text.Label}" Margin="0,0,0,4" />
                        <PasswordBox x:Name="ConfirmPinBox" MaxLength="4" Margin="0,0,0,12"
                                     PasswordChanged="OnConfirmPinChanged" PreviewTextInput="OnPinPreviewTextInput" />
                        <TextBlock Text="{Binding PinMessage}" Style="{StaticResource Text.Caption}" TextWrapping="Wrap" Margin="0,0,0,12"
                                   Foreground="{Binding PinMessageIsError, Converter={StaticResource BoolToNoticeBrush}}" />
                        <Button Content="PIN 변경" Style="{StaticResource Button.Primary}"
                                Command="{Binding ChangePinCommand}" HorizontalAlignment="Left" />
                    </StackPanel>
                </Border>

                <!-- ══════════ 관리자 도구 (power) ══════════ -->
                <Border Style="{StaticResource Card}" Margin="0,0,0,16"
                        Visibility="{Binding IsAdmin, Converter={StaticResource BoolToVis}}">
                    <StackPanel>
                        <TextBlock Text="관리자 도구" Style="{StaticResource Text.H2}" Margin="0,0,0,16" />
                        <StackPanel Orientation="Horizontal">
                            <Button Content="사용자 관리 열기" Style="{StaticResource Button.Secondary}"
                                    Command="{Binding OpenUserManagementCommand}" Margin="0,0,12,0" />
                            <Button Content="앱 종료" Style="{StaticResource Button.Danger}"
                                    Command="{Binding ExitAppCommand}" />
                        </StackPanel>

                        <!-- it13 §7.7: 임시 유저 전역 한도(Admin 전용). 시간·횟수 수정 후 저장. -->
                        <StackPanel Visibility="{Binding CanEditTempUserLimits, Converter={StaticResource BoolToVis}}">
                            <Border Height="1" Background="{StaticResource Brush.Divider}" Margin="0,16,0,12" />
                            <TextBlock Text="임시 유저 한도" Style="{StaticResource Text.H2}" Margin="0,0,0,4" />
                            <TextBlock Text="임시 유저의 QR 전송 한도(전역). 계정 생성 시각부터 적용됩니다."
                                       Style="{StaticResource Text.Caption}" Foreground="{StaticResource Brush.Text.Muted}"
                                       TextWrapping="Wrap" Margin="0,0,0,12" />
                            <Grid Margin="0,0,0,8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="사용 시간(h)" Style="{StaticResource Text.Label}"
                                           VerticalAlignment="Center" Margin="0,0,16,0" />
                                <TextBox Grid.Column="1" HorizontalAlignment="Left" Width="140"
                                         Text="{Binding TempUserQrHours}" />
                            </Grid>
                            <Grid Margin="0,0,0,12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="사용 횟수" Style="{StaticResource Text.Label}"
                                           VerticalAlignment="Center" Margin="0,0,16,0" />
                                <TextBox Grid.Column="1" HorizontalAlignment="Left" Width="140"
                                         Text="{Binding TempUserQrCount}" />
                            </Grid>
                            <TextBlock Text="{Binding TempUserLimitsMessage}" Style="{StaticResource Text.Caption}"
                                       TextWrapping="Wrap" Margin="0,0,0,12"
                                       Foreground="{Binding TempUserLimitsMessageIsError, Converter={StaticResource BoolToNoticeBrush}}" />
                            <Button Content="한도 저장" Style="{StaticResource Button.Primary}"
                                    Command="{Binding SaveTempUserLimitsCommand}" HorizontalAlignment="Left" />
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- ③ 닫기 -->
                <Button Content="닫기" Style="{StaticResource Button.Ghost}"
                        Command="{Binding CloseCommand}" HorizontalAlignment="Left" Margin="0,0,0,16" />
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
