/**
 * 촬영 타이밍 상수 — `analysis/13 §13` (WD18: **하나도 바꾸지 않는다**)
 *
 * 도메인에 두는 이유: 촬영 시퀀스·카메라 테스트 모달이 **같은 값**을 써야 하고
 * (테스트 모달의 목적이 "실촬영과 동일한 재현"이다), 값이 흩어지면 한쪽만 바뀐다.
 */

/** 플래시(화면 하양 오버레이) 지속 시간. 하드웨어 플래시 제어는 없다. */
export const FLASH_DURATION_MS = 120;

/** 컷 사이 대기. 손님이 자세를 바꿀 최소 시간이다. */
export const CUT_INTERVAL_MS = 300;

/** 카운트다운 표시 갱신 주기(표시용 — 판정은 실경과로 한다, WM3). */
export const COUNTDOWN_TICK_MS = 100;
