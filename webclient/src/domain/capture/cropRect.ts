/** 픽셀 사각형 — Windows `Capture/CropCalculator.cs`의 `CropRect` record struct 대응. */
export interface CropRect {
  readonly x: number;
  readonly y: number;
  readonly width: number;
  readonly height: number;
}
