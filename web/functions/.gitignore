# 의존성·빌드 산출물
node_modules/
lib/

# 시크릿(절대 커밋 금지) — 로컬 개발/Emulator용 .env
.env
.env.*
!.env.example
# 에뮬레이터 전용 시크릿 값(defineSecret 오버라이드). 절대 커밋 금지.
.secret.local
*.local

# 로그
*.log
firebase-debug.log
firestore-debug.log
storage-debug.log
ui-debug.log

# Emulator/런타임 캐시
.firebase/
